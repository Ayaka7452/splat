# v1.0.1 修复说明

基于 [antimatter15/splat](https://github.com/antimatter15/splat)（MIT License）的增强版
WebGL 高斯泼溅查看器，支持 `.ply` / `.splat` / `.spz` 格式。

## 问题修复

- **修复移动相机后环绕旋转瞬间吸附回模型中心的问题**
  - 原实现每一步环绕都用 `lookAt` 硬性重建视线，移动相机位置后再旋转，
    第一次拖拽会把视角瞬间掰回正对模型中心
  - 现改为刚性环绕（rigid orbit）：相机先绕自身轴原地旋转，再用同一世界旋转
    把相机位置绕模型中心甩过去，模型中心始终钉在它当前所在的屏幕位置
  - 未移动相机时行为与之前完全一致（正对中心环绕）；移动相机后从当前位置
    继续绕中心旋转，不再吸附
  - 鼠标拖拽、滚轮、单指触摸、`I`/`J`/`K`/`L` 键环绕全部生效

---

# v1.0.0 魔改版发布说明

基于 [antimatter15/splat](https://github.com/antimatter15/splat)（MIT License）的增强版
WebGL 高斯泼溅查看器，支持 `.ply` / `.splat` / `.spz` 格式。

## 新增功能

- **SPZ 格式全版本支持（v1–v4）**
  - v1 / v2 / v3（gzip 压缩）与 v4（ZSTD 多流压缩）
  - 内置纯 JS ZSTD 解码器（fzstd），gzip 走浏览器原生解压，支持 zlib / raw-deflate 回退
  - 加载失败给出具体错误信息
- **左右 / 上下翻转按钮**（快捷键 `H` / `F`），两个开关独立可叠加
- **以模型为中心的环绕旋转**
  - 以点云中位数作为模型中心（抗离群点），相机始终环绕中心旋转并正对模型
- **移动端手势方向修正**
  - 双指平移 / 旋转、单指环绕与翻转状态联动，开启翻转后手势方向依然符合直觉
- **控制面板折叠**（右上角 `−` 小按钮，默认展开）+ **关闭文件**按钮

## 问题修复

- 修复部分浏览器下 gzip 解压报错被吞掉（"The operation was aborted"）的问题
- 修复加载 `cameras.json` 后仍使用旧相机内参的问题

## 精简

- 移除蓝色立方体加载动画
- 清理调试日志、冗余计算与死代码

## 说明

- 本版本基于上游较早期快照魔改，上游新版 `?url=` 远程 `.splat` 加载功能未包含
- 保持原版 MIT License
